IFLYTEK_APP_ID="replace-me"
IFLYTEK_API_KEY="replace-me"
IFLYTEK_API_SECRET="replace-me"
OPENAI_API_KEY="replace-me"
DEEPL_AUTH_KEY="replace-me"

# Google Cloud Translation uses ADC rather than a dedicated app secret.
# Set GOOGLE_APPLICATION_CREDENTIALS to a service-account JSON file path,
# or authenticate with `gcloud auth application-default login`.
